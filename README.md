# AI Financial Analytics - Финансовый аналитик (Adesk + 1С)

Система для анализа финансовых данных из Adesk и 1С: аналитика, прогнозы, рекомендации и риск-скоринг.

## Возможности MVP

- 📊 **Импорт данных**: Adesk XLS и выгрузки 1С (продажи, закупки, ДЗ/КЗ)
- 📈 **Аналитика**: дашборды с метриками, графиками и разрезами
- 🔮 **Прогнозы**: прогноз cash-flow на 7/14/30 дней с выявлением разрывов
- 💡 **Рекомендации**: автоматические рекомендации с обоснованием
- ⚠️ **Риск-скоринг**: оценка кассовых, контрагентских и аномальных рисков
- 🤖 **LLM-чат**: объяснение цифр и трендов через RAG

## Технологический стек

- **Backend**: FastAPI, SQLAlchemy, PostgreSQL
- **Frontend**: Streamlit
- **LLM**: OpenAI API / локальная модель
- **Vector DB**: ChromaDB

## Установка

### Требования

- Python 3.10+
- PostgreSQL 12+

### Шаги установки

1. Клонируйте репозиторий (или создайте проект)

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости backend:
```bash
cd backend
pip install -r requirements.txt
```

4. Установите зависимости frontend:
```bash
cd ../frontend
pip install -r requirements.txt
```

5. Настройте PostgreSQL:
```bash
# Создайте базу данных
createdb financial_analytics

# Или через psql:
psql -U postgres
CREATE DATABASE financial_analytics;
```

6. Настройте переменные окружения:
```bash
# Создайте .env файл в backend/
cd backend
cat > .env << EOF
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/financial_analytics
OPENAI_API_KEY=your-api-key-here
DEBUG=True
EOF
```

7. Примените миграции базы данных:
```bash
cd backend
alembic upgrade head
```

8. Создайте директории для данных:
```bash
mkdir -p data/raw data/processed data/chroma_db
```

## Запуск

### Backend (FastAPI)

```bash
cd backend
uvicorn app.main:app --reload --port 8000
```

API будет доступен по адресу: http://localhost:8000
Документация API: http://localhost:8000/docs

### Frontend (Streamlit)

```bash
cd frontend
streamlit run app.py
```

Приложение будет доступно по адресу: http://localhost:8501

## Использование

### 1. Импорт данных

1. Откройте страницу **Import** в Streamlit
2. Загрузите файл Adesk (XLS "Журнал операций")
3. Загрузите файлы 1С (продажи, закупки, ДЗ/КЗ)
4. Проверьте отчёт качества данных
5. Подтвердите импорт

### 2. Просмотр аналитики

1. Откройте страницу **Dashboard**
2. Выберите период и фильтры
3. Изучите метрики и графики

### 3. Прогнозы

1. Откройте страницу **Forecast**
2. Выберите горизонт прогноза (7/14/30 дней)
3. Просмотрите прогноз CF и остатков
4. Проверьте даты возможных разрывов

### 4. Рекомендации

1. Откройте страницу **Recommendations**
2. Изучите список рекомендаций с приоритетами
3. Просмотрите детали каждой рекомендации

### 5. Риски

1. Откройте страницу **Risks**
2. Просмотрите общий риск-скоринг
3. Изучите детали по категориям рисков

### 6. LLM-чат

1. Используйте чат-виджет на любой странице
2. Задавайте вопросы о данных
3. Получайте объяснения цифр и трендов

## Конфигурация

### Маппинг колонок

Отредактируйте `config/column_mapping.yaml` для настройки соответствия колонок в ваших файлах.

### Маппинг категорий

Отредактируйте `config/category_mapping.yaml` для настройки правил классификации статей.

## Структура проекта

```
project/
├── backend/          # FastAPI backend
├── frontend/         # Streamlit frontend
├── config/           # Конфигурационные файлы
├── data/             # Данные (raw, processed, chroma_db)
└── tests/            # Тесты
```

## Разработка

### Запуск тестов

```bash
cd backend
pytest tests/
```

### Миграции базы данных

```bash
cd backend
# Создать новую миграцию
alembic revision --autogenerate -m "description"

# Применить миграции
alembic upgrade head

# Откатить миграцию
alembic downgrade -1
```

## Лицензия

MIT

## Контакты

Для вопросов и предложений создайте issue в репозитории.
